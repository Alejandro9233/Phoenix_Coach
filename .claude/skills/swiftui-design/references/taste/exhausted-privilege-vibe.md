# Motion-blur portrait ("What a privilege it is to be exhausted…") — exhausted-privilege-vibe.jpg

**Alex:** the effect the app should transmit. Tone, not layout.

**Observed:** monochrome portrait smeared by horizontal motion blur, mono
typewriter caption spaced across the frame, tiny uppercase metadata pinned to
the four corners. The message is about chosen effort. Motion as a texture,
not an animation. The corner-pinned micro-labels are a layout idea worth
stealing for screen headers and empty states.
