# Particle terrain chart with mono metadata — data-mountain-vibe.jpg

**Alex:** the effect the app should transmit. Tone, not layout.

**Observed:** data drawn as a point-cloud mountain, white dust on black,
density instead of a clean line. Tiny mono numbers and labels scattered as
texture around it, one big figure ("-8/706") set apart. It reads as an
instrument, not a dashboard. Directly relevant to Phoenix's charts: load,
fitness, and pace could be drawn as density and grain instead of smooth
strokes.
